name: pw.block.shared.name
icon: shared-block
fields:
  sharedId:
    type: select
    label: pw.block.shared.field.label
    help: pw.block.shared.field.help
    required: true
    options:
      type: query
      query: site.sharedblocks.toBlocks
      text: "{{ item.sharedname }}"
      value: "{{ item.id }}"
