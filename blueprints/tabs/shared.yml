---
label: pw.tab.sharedblocks
icon: shared-block
columns:
  - width: 1/1
    sections:
      shared:
        type: fields
        fields:
          sharedblocks:
            type: blocks
            label: pw.field.blocks
            empty: pw.field.blocks.empty
